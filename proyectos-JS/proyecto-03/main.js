let velocidad = 60;
const limite = 80;

if (velocidad > limite) {
  console.log(
    "Tienes que reducir la velocidad!! Vas a " +
      velocidad +
      "km/h y el limite es de 80"
  );
} else {
  console.log("Puedes seguir a esa velocidad, vas bien !!");
}
